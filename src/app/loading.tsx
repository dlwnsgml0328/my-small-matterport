import React from 'react';

const BasicLoading = () => {
  return <h2>Loading...</h2>;
};

export default BasicLoading;

// framework 에 의해 호출되는 loading component
// loading component는 suspense가 발생했을 때 호출되는 컴포넌트이다.
